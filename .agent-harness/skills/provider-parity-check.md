# Provider parity check

Use when provider configuration or model calls change. Compare readiness, model selection, embedding dimensions, cache namespace, concurrency, error mapping, cancellation, and secret handling for Ollama and OpenAI. Paid provider calls are not required unless explicitly requested.
