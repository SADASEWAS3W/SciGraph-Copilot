# Graph contract check

Use when GraphRAG output, conversion, API data, graph rendering, inspector fields, or chat grounding changes. Trace every changed field from parquet through JSON and API consumers. Missing partial artifacts are allowed during indexing; malformed published artifacts are not.
